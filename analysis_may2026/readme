Results1 folder: BBDuk (adapters, homopolymers), Prinseq low-quality removal and duplicates, human reads removal (bmtagger), read merging (bbmap). Keep only files with 900,000 reads or higher

Results2 folder: BBDuk (adapters, homopolymers), Prinseq low-quality removal and duplicates, human reads removal (bmtagger), skipping read merging (bbmap). Keep only files with 900,000 reads or higher
